export class Calculator {
    sumar(fistNumber: number, secondNumber: number): number {
        return fistNumber + secondNumber;
    }

    restar(fistNumber: number, secondNumber: number): number {
        return fistNumber - secondNumber;
    }
}